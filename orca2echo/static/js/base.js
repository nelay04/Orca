// Check screen width to identify desktop devices
if (window.innerWidth > 1024) { // Assuming screen widths greater than 1024px are desktops
  window.location.href = "/mobile-only"; // Redirect to a mobile-only page (adjust the URL as needed)
}

